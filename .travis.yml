sudo: false
language: node-js
before_install:
services:
  - redis-server
install:
  - npm install
before_script:
  - npm i -g sails@1.0.2
  - npm i @gmod/jbrowse@1.15.2
  - ./setup.sh
script:
  - npm test
after_failure:
  - cat JBrowse-1.x.x-dev/setup.log 
